## 是否支持Windows

当前仅支持在`Linux`发行版本下运行`KPlayer`，若你使用的Windows平台更推荐你使用 `OBS`工具来进行推流。如果为了更低的资源占用下循环推流，`KPlayer`有计划在Windows平台上进行适配。但可能在较久的以后



## 是否支持硬件加速

当前`KPlayer` 优先支持的目标应用场景为是在低配云服务器上循环推流视频资源，在配合缓存下可极大降低CPU与内存的占用情况。在这个场景完善后，将会支持GPU服务器上的硬件解码环境。以便在实时编解码场景下效果更优